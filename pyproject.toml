[build-system]
requires = ["setuptools>=40.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "projectforge"
version = "0.1.0"
description = "A Python project scaffolding tool"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]